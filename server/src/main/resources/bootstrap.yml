spring:
  application:
    name: order
  jpa:
    show-sql: true
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: test

PRODUCT:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule
